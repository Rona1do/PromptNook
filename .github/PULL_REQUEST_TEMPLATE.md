## Problem

What user or maintainer problem does this change solve?

## Solution

Describe the implementation and important tradeoffs.

## Verification

- [ ] `npm test`
- [ ] `npm run build`
- [ ] `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check`
- [ ] `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml`
- [ ] I added or updated tests where behavior changed.
- [ ] I used sanitized data in screenshots, logs, and fixtures.

## Privacy, storage, and compatibility

- Does this add network behavior?
- Does this change the database, package, backup, or filesystem layout?
- What is the migration and rollback behavior?

## UI evidence

For visible changes, add sanitized before/after screenshots. Otherwise write `Not applicable`.
